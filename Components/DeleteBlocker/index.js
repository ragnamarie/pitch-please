import { StyledDeleteBlocker } from "./StyledDeleteBlocker";

export default function DeleteBlocker() {
  return (
    <StyledDeleteBlocker>
      please release all slots before deleting the team!
    </StyledDeleteBlocker>
  );
}
